import styled from 'styled-components';
import { Box } from 'reflexbox';
import { Colors } from '../../../shared/design-system/core/Tokens';

export const StyledHeaderWrapper = styled(Box)`
  width: 100%;
  height: 100vh;
  background-color: ${Colors.BLUE_GRAY_700};
`;

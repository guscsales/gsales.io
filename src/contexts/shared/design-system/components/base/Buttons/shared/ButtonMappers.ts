import { FontSizes, Spaces } from '@design-system/core/Tokens';

export const ButtonSizeMapper = {
  small: {
    height: '2.4rem',
    padding: `${Spaces.NONE} ${Spaces.ONE}`,
    fontSize: FontSizes.SMALL,
  },
  medium: {
    height: '4rem',
    padding: `${Spaces.NONE} ${Spaces.ONE_HALF}`,
    fontSize: FontSizes.MEDIUM,
  },
  large: {
    height: '6.4rem',
    padding: `${Spaces.NONE} ${Spaces.FOUR}`,
    fontSize: FontSizes.LARGE,
  },
};

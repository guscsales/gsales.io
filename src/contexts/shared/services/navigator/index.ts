type NavigatorItem = {
  copy: string;
  url: string;
};

const navigator: NavigatorItem[] = [
  {
    copy: 'Home',
    url: '/',
  },
  {
    copy: 'Wall',
    url: '/wall',
  },
];

export default navigator;

export enum ValidatorErrors {
  RequiredField = 'This field is required',
  InvalidEmail = 'Invalid email',
  NotStrongPassword = 'Your password is not strength enough',
  PasswordMismatch = 'The passwords must be equals',
  InvalidThonLabsPlan = 'Invalid plan',
}
